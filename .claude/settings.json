{
  "skillOverrides": {
    "using-cmux": "off",
    "elevens-investigate": "off"
  },
  "enabledPlugins": {
    "cmux-team@hummer98-cmux-team": false,
    "using-cmux@hummer98-using-cmux": false
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^(Write|Edit)$",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); (print('.team/tasks/ への直接書き込みは禁止です。cmux-team create-task / cmux-team update-task CLI を使用してください。', file=sys.stderr) or sys.exit(2)) if ('.team/tasks/' in p and '/runs/' not in p and not p.endswith('sessions.json')) else sys.exit(0)\"",
            "timeout": 3000
          }
        ]
      }
    ]
  }
}
