{
  "name": "elevens",
  "version": "0.11.0",
  "description": "c11-native multi-agent orchestration package (successor to cmux-team). Spawn, monitor, and coordinate parallel Claude Code sub-agents on Stage 11 Agentics' c11 terminal multiplexer.",
  "author": {
    "name": "hummer98",
    "url": "https://github.com/hummer98"
  },
  "homepage": "https://github.com/hummer98/elevens",
  "repository": "https://github.com/hummer98/elevens",
  "license": "MIT",
  "keywords": ["c11", "stage-11-agentics", "multi-agent", "orchestration", "parallel", "claude-code"],
  "skills": "./skills/",
  "commands": "./commands/",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); FILE=$(echo \"$INPUT\" | jq -r '.tool_input.file_path // empty'); if echo \"$FILE\" | grep -qE '\\.team/(team|task-state)\\.json$'; then echo 'team.json と task-state.json は daemon が管理するファイルです。直接編集しないでください。タスク操作は cmux-team create-task / update-task / close-task コマンドを使用してください。'; exit 2; fi"
          }
        ]
      }
    ]
  }
}
